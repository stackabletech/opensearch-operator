---
apiVersion: opensearch.stackable.tech/v1alpha1
kind: OpenSearchCluster
metadata:
  name: opensearch
spec:
  clusterConfig:
    security:
      config:
        config:
          content:
            value:
              config:
                dynamic:
                  http:
                    anonymous_auth_enabled: false
